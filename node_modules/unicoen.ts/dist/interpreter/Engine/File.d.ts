export declare class File {
    private readonly name;
    private readonly data;
    private readonly mode;
    static setFilelist(filelist: Map<string, ArrayBuffer>): void;
    static getFileFromFileList(filename: string): ArrayBuffer;
    static addFileToFileList(name: string, file: ArrayBuffer): Map<string, ArrayBuffer>;
    private static filelist;
    private pos;
    private buf;
    private toBeFlush;
    constructor(name: string, data: ArrayBuffer, mode: string);
    flush(): void;
    fclose(): void;
    fputc(c: number): number;
    fgetc(): number;
    fgets(n: number): number[];
    private resize;
    private isBinaryMode;
    private isWriteMode;
    private isEOF;
}
