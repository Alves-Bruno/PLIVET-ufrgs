import { UniExpr } from './UniExpr';
export declare class UniUnaryOp extends UniExpr {
    operator: string;
    expr: UniExpr;
    constructor();
    constructor(operator: string, expr: UniExpr);
    toString(): string;
    equals(obj: any): boolean;
    merge(that: UniUnaryOp): void;
}
