import { UniExpr } from './UniExpr';
export declare class UniIdent extends UniExpr {
    name: string;
    constructor();
    constructor(name: string);
    toString(): string;
    equals(obj: any): boolean;
    merge(that: UniIdent): void;
}
