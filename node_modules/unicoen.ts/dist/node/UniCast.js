"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.UniCast = void 0;
const UniExpr_1 = require("./UniExpr");
class UniCast extends UniExpr_1.UniExpr {
    constructor(type, value) {
        super();
        if (type === undefined && value === undefined) {
            this.type = null;
            this.value = null;
        }
        else if (type === undefined || value === undefined) {
            throw new Error('invalid arguments');
        }
        else {
            this.type = type;
            this.value = value;
        }
        this.fields.set('type', String);
        this.fields.set('value', UniExpr_1.UniExpr);
    }
    toString() {
        return 'Cast(' + this.type + ')';
    }
    equals(obj) {
        if (obj == null || !(obj instanceof UniCast))
            return false;
        const that = obj;
        return super.equals(that)
            && (this.type == null ? that.type == null : this.type === that.type)
            && (this.value == null ? that.value == null : this.value.equals(that.value));
    }
    merge(that) {
        super.merge(that);
        if (that.type != null) {
            this.type = that.type;
        }
        if (that.value != null) {
            this.value = that.value;
        }
    }
}
exports.UniCast = UniCast;
//# sourceMappingURL=UniCast.js.map