import { CodeRange } from '../node_helper/CodeRange';
export declare abstract class UniNode {
    comments: string[];
    codeRange: CodeRange;
    fields: Map<string, Function>;
    constructor();
    constructor(comments: string[], codeRange: CodeRange);
    toString(): string;
    equals(obj: any): boolean;
    merge(that: UniNode): void;
}
