"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.UniArray = void 0;
const UniExpr_1 = require("./UniExpr");
class UniArray extends UniExpr_1.UniExpr {
    constructor(items) {
        super();
        if (items === undefined) {
            this.items = [];
        }
        else {
            this.items = items;
        }
        this.fields.set('items', UniExpr_1.UniExpr);
    }
    toString() {
        return 'Array(' + ')';
    }
    equals(obj) {
        if (obj == null || !(obj instanceof UniArray))
            return false;
        const that = obj;
        return super.equals(that)
            && (this.items == null ? that.items == null : this.items.equals(that.items));
    }
    merge(that) {
        super.merge(that);
        if (that.items != null) {
            if (this.items == null) {
                this.items = that.items;
            }
            else {
                this.items.push(...that.items);
            }
        }
    }
}
exports.UniArray = UniArray;
//# sourceMappingURL=UniArray.js.map