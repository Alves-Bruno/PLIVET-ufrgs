"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.UniReturn = void 0;
const UniStatement_1 = require("./UniStatement");
const UniExpr_1 = require("./UniExpr");
class UniReturn extends UniStatement_1.UniStatement {
    constructor(value) {
        super();
        if (value === undefined) {
            this.value = null;
        }
        else {
            this.value = value;
        }
        this.fields.set('value', UniExpr_1.UniExpr);
    }
    toString() {
        return 'Return(' + ')';
    }
    equals(obj) {
        if (obj == null || !(obj instanceof UniReturn))
            return false;
        const that = obj;
        return super.equals(that)
            && (this.value == null ? that.value == null : this.value.equals(that.value));
    }
    merge(that) {
        super.merge(that);
        if (that.value != null) {
            this.value = that.value;
        }
    }
}
exports.UniReturn = UniReturn;
//# sourceMappingURL=UniReturn.js.map