"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.UniIdent = void 0;
const UniExpr_1 = require("./UniExpr");
class UniIdent extends UniExpr_1.UniExpr {
    constructor(name) {
        super();
        if (name === undefined) {
            this.name = null;
        }
        else {
            this.name = name;
        }
        this.fields.set('name', String);
    }
    toString() {
        return 'Ident(' + this.name + ')';
    }
    equals(obj) {
        if (obj == null || !(obj instanceof UniIdent))
            return false;
        const that = obj;
        return super.equals(that)
            && (this.name == null ? that.name == null : this.name === that.name);
    }
    merge(that) {
        super.merge(that);
        if (that.name != null) {
            this.name = that.name;
        }
    }
}
exports.UniIdent = UniIdent;
//# sourceMappingURL=UniIdent.js.map