import { UniStatement } from './UniStatement';
export declare class UniBlock extends UniStatement {
    blockLabel: string;
    body: UniStatement[];
    constructor();
    constructor(blockLabel: string, body: UniStatement[]);
    toString(): string;
    equals(obj: any): boolean;
    merge(that: UniBlock): void;
}
