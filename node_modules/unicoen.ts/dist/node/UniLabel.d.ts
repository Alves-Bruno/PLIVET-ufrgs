import { UniStatement } from './UniStatement';
export declare class UniLabel extends UniStatement {
    name: string;
    constructor();
    constructor(name: string);
    toString(): string;
    equals(obj: any): boolean;
    merge(that: UniLabel): void;
}
