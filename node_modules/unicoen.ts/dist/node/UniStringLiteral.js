"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.UniStringLiteral = void 0;
const UniExpr_1 = require("./UniExpr");
class UniStringLiteral extends UniExpr_1.UniExpr {
    constructor(value) {
        super();
        if (value === undefined) {
            this.value = null;
        }
        else {
            this.value = value;
        }
        this.fields.set('value', String);
    }
    toString() {
        return 'StringLiteral(' + this.value + ')';
    }
    equals(obj) {
        if (obj == null || !(obj instanceof UniStringLiteral))
            return false;
        const that = obj;
        return super.equals(that)
            && (this.value == null ? that.value == null : this.value === that.value);
    }
    merge(that) {
        super.merge(that);
        if (that.value != null) {
            this.value = that.value;
        }
    }
}
exports.UniStringLiteral = UniStringLiteral;
//# sourceMappingURL=UniStringLiteral.js.map