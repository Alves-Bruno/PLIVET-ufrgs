"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.UniLabel = void 0;
const UniStatement_1 = require("./UniStatement");
class UniLabel extends UniStatement_1.UniStatement {
    constructor(name) {
        super();
        if (name === undefined) {
            this.name = null;
        }
        else {
            this.name = name;
        }
        this.fields.set('name', String);
    }
    toString() {
        return 'Label(' + this.name + ')';
    }
    equals(obj) {
        if (obj == null || !(obj instanceof UniLabel))
            return false;
        const that = obj;
        return super.equals(that)
            && (this.name == null ? that.name == null : this.name === that.name);
    }
    merge(that) {
        super.merge(that);
        if (that.name != null) {
            this.name = that.name;
        }
    }
}
exports.UniLabel = UniLabel;
//# sourceMappingURL=UniLabel.js.map