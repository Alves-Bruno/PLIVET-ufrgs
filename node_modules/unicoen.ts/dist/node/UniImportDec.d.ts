import { UniDecralation } from './UniDecralation';
export declare class UniImportDec extends UniDecralation {
    name: string;
    ondemand: string;
    constructor();
    constructor(name: string, ondemand: string);
    toString(): string;
    equals(obj: any): boolean;
    merge(that: UniImportDec): void;
}
