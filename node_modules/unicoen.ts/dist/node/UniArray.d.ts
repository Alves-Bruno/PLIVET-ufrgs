import { UniExpr } from './UniExpr';
export declare class UniArray extends UniExpr {
    items: UniExpr[];
    constructor();
    constructor(items: UniExpr[]);
    toString(): string;
    equals(obj: any): boolean;
    merge(that: UniArray): void;
}
