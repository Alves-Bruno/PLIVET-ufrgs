import { CodeLocation } from './CodeLocation';
export declare class CodeRange {
    begin: CodeLocation;
    end: CodeLocation;
    constructor(begin: CodeLocation, end: CodeLocation);
    equals(obj: any): boolean;
}
